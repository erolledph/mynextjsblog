# Handle Next.js dynamic routes for Cloudflare Pages with ISR support
# Note: These redirects are now handled by Next.js runtime instead of static files

# API routes (if any are added in the future)
/api/* /api/:splat 200

# Dynamic blog routes - let Next.js handle these
/blog/* /blog/:splat 200

# Static pages
/about /about 200

# Fallback - let Next.js handle all other routes
/* /:splat 200